import { cormorantFont } from "app/layout";

export const fontSize = 18;

export const typography = {
  fontSize,
  htmlFontSize: 18,
  fontFamily: cormorantFont,
  body1: { fontSize },
  body2: { fontSize },
};
